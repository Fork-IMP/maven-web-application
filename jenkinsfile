node{
def mavenHome = tool name: "maven3.8.6"
//properties([buildDiscarder(logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '5', daysToKeepStr: '', numToKeepStr: '5')), pipelineTriggers([cron('* * * * *')])])
//properties([buildDiscarder(logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '5', daysToKeepStr: '', numToKeepStr: '5')), pipelineTriggers([cron('* * * * *')])])
properties([buildDiscarder(logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '5', daysToKeepStr: '', numToKeepStr: '5')), pipelineTriggers([githubPush()])])

stage('pushing the code from scm')
{
git branch: 'development', credentialsId: '804e4050-048d-4409-b8db-8bc607af0a93', url: 'https://github.com/Fork-IMP/maven-web-application.git'
}
    
stage ('genarting the war file from maven')
{
    sh "${mavenHome}/bin/mvn clean package"
}
stage ('gerating report in sonarqube')
{
sh "${mavenHome}/bin/mvn sonar:sonar"
}

stage ('pushing artifacts nexus repo')

{ 
sh "${mavenHome}/bin/mvn deploy"
}
stage('deploye the code into web server')
{
sshagent(['87fa379f-e2ae-4e44-b1f7-5eed5bec15b8']) {
    sh "scp -o StrictHostKeyChecking=no target/maven-web-application.war ec2-user@172.31.92.23:/opt/apache-tomcat-9.0.65/webapps"
}
}


}
